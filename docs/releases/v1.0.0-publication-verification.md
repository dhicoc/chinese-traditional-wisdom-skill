# v1.0.0 正式发布验证记录

> 发布日期：2026-08-21
> Tag：`v1.0.0`
> Tag commit：`3a7dd71d0ae3d64c2d8572f04c0c28b3bc324b39`
> Release：<https://github.com/dhicoc/chinese-traditional-wisdom-skill/releases/tag/v1.0.0>

## 发布候选质量门

- CI：<https://github.com/dhicoc/chinese-traditional-wisdom-skill/actions/runs/32468543910>
- Visual quality gates、Chromium、WebKit、Mobile Chrome、Mobile Safari 全部成功。
- 800 项单测、19 项 Skill eval、16 项 release contracts。
- npm production audit：0 known vulnerabilities。
- Python 3.13 隔离 venv 安装、`pip check` 和 oracle smoke 通过。

## 发布过程发现并修复

1. `postcss → nanoid 3.3.17` 高危公告：override 到 3.3.18，audit 清零。
2. 默认 Python requirements 中不存在的 `ichingshifa==0.1.0`：默认安装改为跨平台 oracle core；原生六爻 oracle 改为可选。
3. `iztro-py` 历史 helper API：适配 0.5.0。
4. Windows CRLF checkout 导致 release contract 误报：改用 CRLF/LF 兼容正则后，重新 CI、重新创建最终 tag。

## Tag 冷克隆 Canary

从远端 `v1.0.0` 全新克隆，在隔离 Python venv 中运行真实 `scripts/setup.bat`：

- Python dependencies 与 import smoke：通过；
- TypeScript frozen install 与 typecheck：通过；
- engine:list：32；
- engine:plan：通过；
- Skill eval：19/19；
- release contracts：16/16；
- build：通过；
- Chromium smoke：8/8。

## Release 资产

- `chinese-traditional-wisdom-skill-v1.0.0.zip`
- `chinese-traditional-wisdom-dashboard-v1.0.0.zip`
- `release-manifest-v1.0.0.json`
- `SHA256SUMS.txt`

全部资产下载后 SHA-256 一致。源码包不含 node_modules 或 `.env`；Dashboard 包包含 index.html 和 verify.html。

## 静态资产 Canary

直接解压已发布 Dashboard 资产并使用独立静态服务器加载：

- app shell：可见；
- 24 个 tab：存在；
- 八字工作区：可打开；
- verify.html：HTTP 200；
- 控制台错误：0。

结论：✅ v1.0.0 正式发布完成，可以交付。
